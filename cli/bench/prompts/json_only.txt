Extract all action items from the meeting notes below. Return ONLY a JSON object — no prose, no markdown, no code fences. The JSON must conform exactly to this schema:

{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "required": ["action_items"],
  "properties": {
    "action_items": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["owner", "task"],
        "additionalProperties": false,
        "properties": {
          "owner": { "type": "string" },
          "task":  { "type": "string" }
        }
      }
    }
  },
  "additionalProperties": false
}

Meeting notes:
---
{{MEETING_NOTES}}
---

Output only the JSON object. No other text.
