# Local dev artifacts
.DS_Store
*.swp
*.swo
*~

# Editor / IDE
.idea/
.vscode/

# Secrets — should never be committed
.env
.env.*
!.env.example

# Local test output
tmp/
*.log

# D2: fault records — may contain process input data (see SPEC §11.4)
# Never commit fault records: they are local-only diagnostic artifacts.
**/*.fault.json
.opensop/faults/
